{
  "name": "setu",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @causeway-sync/relay-server && npm run build -w @causeway-sync/core && npm run build -w @causeway-sync/bench",
    "lint": "eslint .",
    "format": "prettier --check .",
    "test": "npm run test -w @causeway-sync/relay-server && npm run test -w @causeway-sync/core"
  },
  "devDependencies": {
    "@eslint/js": "^9.31.0",
    "@types/node": "^24.0.14",
    "eslint": "^9.31.0",
    "prettier": "^3.6.2",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.37.0",
    "vitest": "^3.2.4"
  }
}
